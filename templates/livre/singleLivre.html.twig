{% extends 'base.html.twig' %}

{% block title %}{% endblock %}
{% block aside %}
  <div class="card my-5 mx-5" style="width: 18rem;">
    <div class="card-body">
      <h5 class="card-title">{{ livre.titre }}</h5>
        <ul class="card-text">
          <li>Auteur : {{ livre.auteur }}</li>
          <li>Resumé : {{ livre.resume }}</li>
          <li>Date : {{ livre.dateParution|date('d/m/Y') }}</li>
          <li>Status :
          {% if livre.status == 0 %} {{"Indisponible"}}
          {% else %}
          {{"Disponible"}}
          {% endif %}</li>
          <li>Category : {{ livre.category.name }}</li>
        </ul>
      <a href="#" class="btn bg-warning">Modifier</a>
      <a href="{{ path('app_emprunt', {'id' : livre.id}) }}" class="btn bg-secondary">Emprunter</a>
    </div>
  </div>
{% endblock %}
